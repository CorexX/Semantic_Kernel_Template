# requirements.txt
# This file lists the Python dependencies required for the Semantic Kernel Orchestration system.
# Install with: pip install -r requirements.txt

python-dotenv
azure-identity
python-docx
semantic_kernel
